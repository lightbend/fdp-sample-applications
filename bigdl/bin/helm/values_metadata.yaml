image:
  __metadata:
    label: "Information about used images"
  vggcifar:
    __metadata:
      label: "VGG training on cifar-10 data image"
      description: "Name of the VGG training on cifar-10 data image"
      type: "string"
      required: true
  pullPolicy:
    __metadata:
      label: "Image pull policy"
      description: "Image pull policy"
      type: "string"
      required: true
  version:
    __metadata:
      label: "Image version"
      description: "Image version should correspond to the FDP version"
      type: "string"
      required: true
vggcifar:
  __metadata:
    label: "Details for the VGG on cifar training component"
  jar:
    __metadata:
      label: "JAR name"
      description: "Name of the jar used for application"
      type: "string"
      required: true
  main:
    __metadata:
      label: "main class"
      description: "Fully qualified name of the main class used for application"
      type: "string"
      required: true
