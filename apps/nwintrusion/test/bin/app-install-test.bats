#!/usr/bin/env bats

test_support=test/support
normal_properties="${test_support}/normal.app-install.properties"

@test "todo" {
  echo "placeholder for nwintrusion app tests"

  # [ $status -eq 0 ]

  # [[ "${lines[0]}" =~ "ERROR" ]]
}
