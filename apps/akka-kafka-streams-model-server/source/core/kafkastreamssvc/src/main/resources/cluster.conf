kafka.brokers = ${?KAFKA_BROKERS_LIST}
queriable_state.port = 8880
queriable_state.port = ${?MODEL_SERVER_PORT}

grafana {
  host = ${?GRAFANA_HOST}
  port = "3000"
  port = ${?GRAFANA_PORT}
}

influxdb {
  host = ${?INFLUXDB_HOST}
  port = "8086"
  port = ${?INFLUXDB_PORT}
}
